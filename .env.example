# Copy to .env (dev) or enter keys in Ada Settings after install.
# Keys are never baked into Ada_*-setup.exe.

OPENAI_API_KEY=
XAI_API_KEY=

# Optional model overrides
# OPENAI_CHAT_MODEL=gpt-5.4
# OPENAI_ROUTING_MODEL=gpt-5-mini
# XAI_CHAT_MODEL=grok-4-1-fast-non-reasoning
# XAI_ROUTING_MODEL=grok-4-1-fast-non-reasoning
